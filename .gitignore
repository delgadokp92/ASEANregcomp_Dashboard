# Python
__pycache__/
*.pyc
.venv/
venv/

# Streamlit — track config.toml (theme), ignore secrets only
.streamlit/secrets.toml

# Data (avoid committing sensitive files)
*.xlsx
*.csv
# Allow category source data
!src/categories/*.csv

# OS
.DS_Store
